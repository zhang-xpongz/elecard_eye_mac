# Phase D 启用 — 现在保持空让顶层 add_subdirectory 通过
# add_executable(streameye_app main.cpp main_window.cpp)
# target_link_libraries(streameye_app PRIVATE streameye_model streameye_decoder
#                                              Qt6::Widgets Qt6::Concurrent)
